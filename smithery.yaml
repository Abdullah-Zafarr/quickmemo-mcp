startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      storagePath:
        type: string
        description: "Custom local path for storing memos JSON file (defaults to ~/.quickmemo/memos.json)"
  commandFunction:
    - uvx
    - "--from"
    - "."
    - "quickmemo"
